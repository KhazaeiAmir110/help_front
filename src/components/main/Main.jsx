import React from 'react';
import MoviesList from "../movies/MoviesList.jsx";

function Main(props) {
    return (
        <main>
            <MoviesList />
        </main>
    );
}

export default Main;